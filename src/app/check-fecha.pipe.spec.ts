import { CheckFechaPipe } from './check-fecha.pipe';

describe('CheckFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
